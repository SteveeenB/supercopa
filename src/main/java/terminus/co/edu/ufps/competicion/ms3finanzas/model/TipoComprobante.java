package terminus.co.edu.ufps.competicion.ms3finanzas.model;

/**
 * Tipo de comprobante que un delegado puede subir.
 * INSCRIPCION → pago del cupo del equipo al torneo (HU25).
 * MULTA       → pago de una multa generada por tarjeta (HU27).
 */
public enum TipoComprobante {
    INSCRIPCION,
    MULTA
}
