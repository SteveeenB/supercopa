spring.application.name=CompeticionCodeCup
server.port=${SERVER_PORT:8082}

# Base de datos nueva (PostgreSQL separada para MS2)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres}
spring.datasource.username=${DB_USERNAME:postgres.zfjnyewmrlhtviwnxqew}
spring.datasource.password=${DB_PASSWORD:@Dav.2195sapahpta}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=create

# HikariCP - limit connections
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# JWT: validate with MS1 public key (Pattern B)
codecup.jwt.issuer=${CODECUP_JWT_ISSUER:http://localhost:8081}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${codecup.jwt.issuer}/.well-known/jwks.json
spring.security.oauth2.resourceserver.jwt.issuer-uri=${codecup.jwt.issuer}

app.frontend-url=${FRONTEND_URL:http://localhost:5173}

springdoc.swagger-ui.path=/swagger-ui/index.html
